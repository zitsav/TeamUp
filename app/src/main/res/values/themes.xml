<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TeamUp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="RoundedButton">
        <item name="android:padding">12dp</item> <!-- Adjust the padding to your preference -->
        <item name="android:textColor">@android:color/white</item> <!-- Set the text color -->
        <item name="android:background">@drawable/rounded_button_background</item>
    </style>


    <style name="Theme.TeamUp" parent="Base.Theme.TeamUp" />
</resources>